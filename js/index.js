// 1ST TASK

let a = 3;
let b = 5;
let c;

// Printing
console.log(
  `a + b = ${a + b}\n` +
  `a - b = ${a - b}\n` +
  `a * b = ${a * b}\n` +
  `a / b = ${a / b}\n` +
  `a % b = ${a % b}\n` +
  `a += b = ${a += b}\n` + 
  `a -= b = ${a -= b}\n` +
  `a *= b = ${a *= b}\n` +
  `a /= b = ${a /= b}\n` +
  `a %= b = ${a %= b}\n` +
  `a == b = ${a == b}\n` +
  `a != b = ${a != b}\n` +
  `a > b = ${a > b}\n` +
  `a < b = ${a < b}\n` +
  `!a && !c = ${!a && !c}\n` +
  `!a || !c = ${!a || !c}` 
);

// 2ND TASK
// DECLARING THE VARIABLES
let first_name = "Alen";
let last_name = "Antony";
let email = "anto0125@algonquinlive.com";
let output;
// ASSIGNING TO OUTPUT AFTER CREATING THE SENTENCE
output = `MY NAME IS ${first_name} ${last_name}. YOU CAN CONTACT ME AT ${email}`;
// PUTTING IT TO CONSOLE
console.log(output);